# PS5 價格追蹤機器人 (PChome)

使用 [browser-use](https://github.com/browser-use/browser-use) 建立的 AI 代理人，能自動在 PChome 搜尋 PS5 主機並進行比價。

## 快速開始
1. 安裝 uv：依照 [uv 官網](https://astral.sh/uv/) 指令安裝。
2. 環境同步：執行 `uv sync`。
3. 設定金鑰：建立 `.env` 檔案並填入 `BROWSER_USE_API_KEY=您的金鑰`。
4. 執行：`uv run main.py`
